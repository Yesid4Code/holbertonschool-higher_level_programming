/**
 * JS that updates the text color of the HTML tag HEADER to red
 */
document.addEventListener('DOMContentLoaded', function () {
  // To runs when the DOM is ready, i.e. when the document has been parsed
  document.querySelector('HEADER').style.color = '#FF0000';
});
